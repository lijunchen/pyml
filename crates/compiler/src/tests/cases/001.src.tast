fn main() -> Unit {
  let _ : Unit = () in
  let a/0: (Bool, Bool, (Bool, Bool)) = (true, false, (true, false)) in
  let b/1: Bool = (a/0 : (Bool, Bool, (Bool, Bool))).2.1 in
  let _ : Unit = print(bool_to_string((b/1 : Bool))) in
  ()
}