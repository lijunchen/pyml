enum Color {
    Red,
    Green,
    Blue
}

let a = (Blue, Blue) in
match a {
    (Red, Green) => true,
    (Red, Red) => true,
    (Blue, Blue) => let _ = print_bool(true) in
      false,
    _ => false,
}