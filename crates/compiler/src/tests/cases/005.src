enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let a = (Blue, Red) in
    match a {
        (Red, Green) => print(int_to_string(0)),
        (Red, Red) => print(int_to_string(1)),
        (Blue, Blue) => print(int_to_string(2)),
        _ => print(int_to_string(3)),
    }
}
