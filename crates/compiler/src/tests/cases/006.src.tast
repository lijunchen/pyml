main () :Unit let a/0 = (true, false);
let _ : Unit = match (a/0 : (Bool, Bool)) {
    (true,b/1) => print_bool( (b/1 : Bool) ),
    _ : (Bool, Bool) => (),
};
let c/2 = (true, true);
match (c/2 : (Bool, Bool)) {
    (true,d/3) => print_bool( (d/3 : Bool) ),
    _ : (Bool, Bool) => (),
}