main () :Unit let a = (true, false);
let _ : Unit = match (a : (Bool, Bool)) {
    (true,b) => print_bool( (b : Bool) ),
    _ : (Bool, Bool) => (),
};
let c = (true, true);
match (c : (Bool, Bool)) {
    (true,d) => print_bool( (d : Bool) ),
    _ : (Bool, Bool) => (),
}