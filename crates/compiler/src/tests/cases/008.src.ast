enum T {
    A,
    B(Bool, Unit)
}

fn main() {
    let t = B(true, ()) in
    match t {
        A => print_int(1),
        B(true, ()) => print_int(2),
        B(false, ()) => print_int(3),
    }
}

